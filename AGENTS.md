# Repository Guidance

`main` is protected. Do not push directly to it.

For changes in this repository, create a descriptive branch, commit the intended files, push that branch, and open a pull request targeting `main`. Required GitHub Actions checks must complete before the pull request can be merged.

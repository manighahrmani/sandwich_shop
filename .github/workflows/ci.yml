name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.x"
      - name: Install pre-commit
        run: pip install pre-commit
      - name: Run pre-commit hooks
        run: pre-commit run --all-files

  dart-lint:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - name: Check for pubspec.yaml
        id: check_pubspec
        run: |
          if [ -f "pubspec.yaml" ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Set up Flutter
        if: steps.check_pubspec.outputs.exists == 'true'
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2.23.0
        with:
          flutter-version: "3.x"
          channel: stable
      - name: Install dependencies
        if: steps.check_pubspec.outputs.exists == 'true'
        run: flutter pub get
      - name: Check Dart formatting
        if: steps.check_pubspec.outputs.exists == 'true'
        run: dart format --set-exit-if-changed .
      - name: Analyse Dart code
        if: steps.check_pubspec.outputs.exists == 'true'
        run: flutter analyze
      - name: Run tests
        if: steps.check_pubspec.outputs.exists == 'true'
        run: flutter test

  link-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - name: Check markdown links
        uses: lycheeverse/lychee-action@e7477775783ea5526144ba13e8db5eec57747ce8 # v2.9.0
        with:
          args: >-
            --no-progress
            --accept 200,201,202,203,204,206,301,302,303,304,307,308,403,429,999
            "**/*.md"
          # TODO(#23): flip to true once the pre-existing broken links are fixed
          fail: false
